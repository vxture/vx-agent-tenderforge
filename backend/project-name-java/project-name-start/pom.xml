<?xml version="1.0" encoding="UTF-8"?>
<!-- GENERATED_BY_AI -->
<!-- MODEL: gpt-5 -->
<!-- DATE: 2026-07-29 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.td</groupId>
        <artifactId>czghagent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>czghagent-start</artifactId>
    <name>czghagent-start</name>
    <dependencies>
        <dependency>
            <groupId>com.td</groupId>
            <artifactId>czghagent-infrastructure</artifactId>
        </dependency>
        <dependency>
            <groupId>com.td</groupId>
            <artifactId>czghagent-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.td.czghagent.Application</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                        @{argLine} 是承重的，不是装饰：JaCoCo 的 prepare-agent 把 -javaagent
                        写进名为 argLine 的属性，而这里显式声明 argLine 会把它整个盖掉。
                        盖掉的表现不是报错，是「一个 exec 文件都没有」——JaCoCo 安静地跳过报告，
                        覆盖率显示为 0，看起来像是没写测试。用 @{} 做延迟求值才能两个 agent 并存。
                    -->
                    <argLine>@{argLine} -javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
