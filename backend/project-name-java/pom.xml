<?xml version="1.0" encoding="UTF-8"?>
<!-- GENERATED_BY_AI -->
<!-- MODEL: gpt-5 -->
<!-- DATE: 2026-07-29 -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.td</groupId>
    <artifactId>czghagent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>czghagent</name>
    <description>TenderAgent 投标文件编制业务服务</description>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <springdoc.version>2.8.13</springdoc.version>
        <!--
            JOSE 实现。只引这一个而不是 spring-boot-starter-oauth2-client：
            后者会把 Spring Security 的整条过滤链拉进来，与现有
            AuthenticationFilter 争夺请求，等于顺手换掉整个安全模型。
            RP 需要的其余部分（discovery、PKCE、换票、会话）都是显式代码，
            唯独签名验证不该自己写。
        -->
        <nimbus-jose-jwt.version>10.0.2</nimbus-jose-jwt.version>
        <!--
            1.27.0 拉进来的 protobuf-java 3.21.7 与 grpc-* 1.54.1 各自带着
            高危告警，且都不在 Spring Boot BOM 的管辖范围内——只能从这里抬。
        -->
        <temporal.version>1.38.0</temporal.version>
        <flyway.version>11.20.3</flyway.version>

        <!--
            以下四个是<b>压过 Spring Boot BOM</b> 的安全下限，不是选型。
            Boot 3.5.16 钉的版本仍有未修的告警，而 `audit` 是硬闸门，
            所以在这里显式抬高；每一条都对应 osv-scanner 实际报出的条目。
            Boot 升级后应逐个回头看：钉的版本追上了就把这条删掉，
            留着一个比 BOM 更低的值反而会把版本按回去。
        -->
        <!--
            9.8 / 9.1 / 9.1 三条，Boot 3.5.16 钉的是 10.1.55。
            告警里写的修复版是 10.1.58，但 Apache <b>跳过了这个版本号</b>——
            Central 上 10.1.57 之后直接是 10.1.59。照着告警写会得到
            「was not found in repo.maven.apache.org」而构建失败，
            而 SBOM 那一步不下载 jar，照样报「0 个漏洞」。
        -->
        <tomcat.version>10.1.59</tomcat.version>
        <!-- 3 条，Boot 3.5.16 钉的是 2.21.4 -->
        <jackson-bom.version>2.21.5</jackson-bom.version>
        <!-- GHSA-j288-q9x7-2f5v，Boot 3.5.16 仍钉 3.17.0 -->
        <commons-lang3.version>3.18.0</commons-lang3.version>
        <!-- GHSA-qv9r-c865-cp47，Boot 3.5.16 仍钉 2.24.3 -->
        <log4j2.version>2.25.5</log4j2.version>
    </properties>

    <modules>
        <module>project-name-domain</module>
        <module>project-name-application-command</module>
        <module>project-name-infrastructure</module>
        <module>project-name-application-query</module>
        <module>project-name-web</module>
        <module>project-name-start</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.td</groupId>
                <artifactId>czghagent-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.td</groupId>
                <artifactId>czghagent-application-command</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.td</groupId>
                <artifactId>czghagent-infrastructure</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.td</groupId>
                <artifactId>czghagent-application-query</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.td</groupId>
                <artifactId>czghagent-web</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <!--
            覆盖率。JaCoCo 装在父 pom 上而不是逐模块装：六个模块各配一次，
            早晚会有一个新模块漏配，而漏配的表现是「那个模块的覆盖率看起来是 100%」
            ——因为它根本没被统计。
        -->
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.13</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--
        跨模块汇总报告。
        单模块报告回答不了「整个后端覆盖了多少」——测试几乎全在 project-name-start，
        而被测代码在另外五个模块里，逐模块看会得到「start 覆盖率很高、其余为零」
        这种既真实又毫无用处的结论。
    -->
    <profiles>
        <profile>
            <id>coverage-report</id>
            <!--
                汇总模块只在这个 profile 下进反应堆：它是一个纯构建期制品，
                没有理由让镜像构建为它多跑一遍解析。
            -->
            <modules>
                <module>project-name-coverage</module>
            </modules>
        </profile>
    </profiles>
</project>
