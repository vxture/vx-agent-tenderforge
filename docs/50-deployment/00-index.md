# 50-deployment 索引

| 文件 | 内容 |
| --- | --- |
| [10-deployment-plan.md](./10-deployment-plan.md) | 部署规划：形态差异、三级密钥划分、SCA 闸门实况、待决策与执行顺序 |
| [rebuild/](./rebuild/) | 可逐字重放的仓库配置（`main-ruleset.json` 等），非文档 |

> 端口数值不在本仓任何文档里——取号唯一源是组织端口登记表。运行态真值在
> `.env` 与 `docker-compose.yml` 的回退默认值里（端口登记表 R2/R3）。
